<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- الإنترنت وحده. ولا كاميرا ولا تخزينٌ في البيان: الكاميرا تُفتح
       من الويب بمحرّك كروم، وهو يطلب إذنها بنفسه عند أوّل استعمال —
       فلا يرى المعلّم قائمةَ أذوناتٍ مخيفةً عند التثبيت. -->
  <uses-permission android:name="android.permission.INTERNET" />

  <application
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher"
      android:supportsRtl="true"
      android:allowBackup="true"
      android:theme="@style/Theme.Midad">

    <activity
        android:name="sa.midad.app.LauncherActivity"
        android:exported="true"
        android:label="@string/app_name"
        android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode">

      <meta-data
          android:name="android.support.customtabs.trusted.DEFAULT_URL"
          android:value="@string/launch_url" />
      <meta-data
          android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
          android:resource="@color/brand" />
      <meta-data
          android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
          android:resource="@color/brand" />
      <!-- شاشةُ الإقلاع: لولاها لرأى المعلّم بياضًا حتّى يُقلع كروم. -->
      <meta-data
          android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
          android:resource="@mipmap/ic_launcher" />
      <meta-data
          android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
          android:resource="@color/brand" />

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <!-- روابطُ الموقع تفتح في التطبيق: يضغط المعلّم رابطًا في واتساب
           فيفتح مِدادَ لا المتصفّح. ويعمل هذا متى صحّ `assetlinks`. -->
      <intent-filter android:autoVerify="true">
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" android:host="@string/host" />
      </intent-filter>
    </activity>

    <!-- خدمةُ التحقّق من ربط النطاق -->
    <service
        android:name="sa.midad.app.DelegationService"
        android:exported="true">
      <intent-filter>
        <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
    </service>
  </application>
</manifest>
