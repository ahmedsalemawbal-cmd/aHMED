/**
 * Osoul Mail — ألوان النافذة على مستوى النظام.
 *
 * وحدة صغيرة مستقلة يشترك فيها main و ipc: لو استوردا الألوان من بعضهما
 * لتكوّنت دورة require تُفرغ الصادرات وقت التحميل.
 */

'use strict';

/** خلفية النافذة قبل رسم الصفحة — تمنع وميضًا أبيض عند الفتح. */
const WINDOW_BG = { dark: '#0b1216', light: '#eef1f3' };

module.exports = { WINDOW_BG };
