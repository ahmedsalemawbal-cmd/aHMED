# مِداد — أعرافُ العمل

أعرافٌ قالها المالك (أحمد) صراحةً، تُتَّبع في كلّ جلسة.

## ① مع كلّ تعديلٍ للتطبيق: أرسِل رابط التحميل

الرابطُ ثابتٌ لا يتغيّر:

    https://github.com/ahmedsalemawbal-cmd/aHMED/releases/download/android-v1.0.0/midad-android.apk

ولذلك **الرابطُ وحده لا يكفي**: يجب أن يُقال معه ما يُثبت أنّ الملفّ
تبدّل فعلًا — وقتُ رفعه وبصمتُه. فالمالك ينزّل من العنوان نفسه في كلّ
مرّة، ولا يميّز الجديدَ من القديم إلّا بذلك.

    الرابطُ الثابت لا يدلّ على التغيّر. الوقتُ والبصمةُ يدلّان.

**ولا يُقال «جاهز» حتّى يُرى الملفّ نفسه** على الإصدار — لا حتّى ينجح
السير. وقد سقط بناءٌ مرّةً بعد أن قيل إنّه سليم: كان «سليمًا» في الحزم
والأنواع، وساقطًا في المشروع الأصليّ. وبيئةُ التطوير هنا لا تبني أندرويد،
فما يُقال عنها لا يُقال عن الـAPK.

التحقّق: `get_release_by_tag` على `android-v1.0.0` → قارن `assets[0].updated_at`
و`digest` بما كانا. وخطوةُ الإصدار آخرُ خطوةٍ في السير ولا تعمل إلّا بعد
بناءٍ ناجح — فملفٌّ جديدٌ يعني بناءً نجح.

## ② اسأل، ثمّ صمّم، ثمّ نفّذ

«اسألني أوّل شيء، بعدين أنا أجاوبك، بعدين أنت تصمّم صورة عن الشكل كيف
بيكون، بعدين أعطيك موافقة أو تعديل، بعدين تبدأ التنفيذ.»

ويُستثنى ما يقول فيه صراحةً «خلاص أكمل» أو «نفّذ».

## ③ إعداداتٌ عامّة لا ترقيعَ ملفٍّ بملفّ

ما يُصلَح يُصلَح في موضعه الواحد الذي يحكم الكلّ — لا في كلّ ملفٍّ على
حدة. وقد ردّ هذا مرارًا حين عُولج عَرَضٌ في موضعه.

## ④ التطبيق للميدان، والموقع للمكتب

القوالبُ والملفّات في الموقع وحده. والتطبيق: رصدٌ وجدولٌ وجداولُ نور
وملفُّ الإنجاز. (قرارُه، وقد حُذفت أربعُ شاشاتٍ عليه.)

## ⑤ اللغة

كلُّ ما يُكتب — شيفرةً وتعليقًا ورسالةَ إيداعٍ وردًّا — بالعربية.
