# انسخ هذا الملف باسم .env.local ثم املأه من لوحة Supabase
# (Project Settings ← API)

VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...

# المفتاح المجهول (anon) آمنٌ في المتصفّح — لأن الحارس في القاعدة
# (RLS) لا في الواجهة. ولا تضع هنا service_role أبدًا: ذاك يتجاوز
# كل سياسة أمان، ووضعه في ملف يُبنى مع الموقع يعني تسليم المدرسة كلها.
