/// <reference types="vite/client" />

/*
 * أنواعُ Vite للعميل — بها يُعرَف `import.meta.env` و`BASE_URL`.
 *
 * وكان `lib/config.ts` يتحايل بـ`(import.meta as any).env`، وهو صمتٌ
 * لا حلّ: يُسكِت المدقّقَ ويترك الخطأَ ليظهر عند التشغيل. وهذا الملفُّ
 * يجعل المدقّقَ يعرف، فيمسك اسمًا مكتوبًا خطأً قبل البناء.
 *
 *     ما يُسكَت عنه في الفحص يُقال في الإنتاج.
 */
